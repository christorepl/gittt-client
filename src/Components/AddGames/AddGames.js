import React from 'react'

export default class AddGames extends React.Component {
    render () {
        return (
            <div className="addGames">
                placeholder page for adding games from a boardgame geek account, board game atlast account, manually, or randomly
            </div>
        )
    }
}